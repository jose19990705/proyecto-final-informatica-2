/*#include "Enemigo.h"

Enemigo::Enemigo(const string& nombre, unsigned short vida_Personaje, unsigned short danio, const QString& spriteEnemigo)
    : Personaje(nombre, vida_Personaje, danio, spriteEnemigo){

    spritePersonaje = new QPixmap(spriteEnemigo);
    *spritePersonaje = spritePersonaje->scaled(20, 30, Qt::KeepAspectRatioByExpanding);  // Definir tamaño del enemigo
    setPixmap(*spritePersonaje);  // Asignamos el sprite al enemigo
    setPos(posX, posY);  // Posición donde se crea el enemigo
}*/
