/*#ifndef ENEMIGO_H
#define ENEMIGO_H

#include <QtWidgets>
#include <QTimer>

#include "Personaje.h"

class Enemigo : public Personaje, public QWidget, public QGraphicsPixmapItem {
public:
    Enemigo(const string& nombre, unsigned short vida_Personaje, unsigned short danio, const QString& spriteEnemigo);
    ~Enemigo();
};

#endif // ENEMIGO_H*/
